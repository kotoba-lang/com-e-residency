{
  "private": true,
  "scripts": {
    "test": "kbb --backend sci --classpath src:test -e \"(require '[clojure.test :as t] 'e-residency.main-test) (let [r (t/run-tests 'e-residency.main-test)] (.exit js/process (if (pos? (+ (:fail r) (:error r))) 1 0)))\""
  }
}
